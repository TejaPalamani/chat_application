import express from 'express';
import { validation } from '../validation/validation';

const MessageRouter = express.Router();

//Messages model just holds the data externally client dont send any req to it

//MessageRouter.post("/sending_mesg" , ) //
